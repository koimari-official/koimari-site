# LINE公式アカウント 構築設計書

作成日: 2026-09-22
対象アカウント: ケーキ屋さん こいまり 城東（LINE公式アカウント）
Basic ID: `@744lgqjn`　/　友だち追加URL: `https://lin.ee/tgXqHoK`

サイト側の改修（トップページの「2つの予約導線」新設）と対になる、LINE側の設計案。実装（LINE Official Account Manager上の設定）はオーナー側での作業が必要です。

---

## 1. Webからの遷移時：初期テキスト入力URLスキーム

LINEは、公式アカウントのトーク画面を開くと同時にメッセージ入力欄へ文字列を差し込める仕組みを公式に提供しています（`oaMessage` URLスキーム）。これを使うと、「何の用件で来たお客様か」が最初から分かった状態でトークが始まります。

### 使い方

```
https://line.me/R/oaMessage/{LINE IDをパーセントエンコード}/?{メッセージをパーセントエンコード}
```

### こいまり向けの具体例

| 用途 | URL |
|------|-----|
| 定番ホールケーキ予約 | `https://line.me/R/oaMessage/%40744lgqjn/?%E3%80%90%E5%AE%9A%E7%95%AA%E3%83%9B%E3%83%BC%E3%83%AB%E3%82%B1%E3%83%BC%E3%82%AD%E4%BA%88%E7%B4%84%E3%80%91` |
| イベント・オーダーケーキ予約 | `https://line.me/R/oaMessage/%40744lgqjn/?%E3%80%90%E3%82%A4%E3%83%99%E3%83%B3%E3%83%88%E3%83%BB%E3%82%AA%E3%83%BC%E3%83%80%E3%83%BC%E3%82%B1%E3%83%BC%E3%82%AD%E4%BA%88%E7%B4%84%E3%80%91` |

タップすると、LINEアプリのトーク画面が開き、入力欄に **「【定番ホールケーキ予約】」** のような文字列が自動で入力された状態になります（お客様が送信ボタンを押すだけで済みます）。

### 採用にあたっての注意点（未検証のため要テスト）

- この方式は「LINEアプリがインストールされている前提」の深いリンクです。`https://lin.ee/tgXqHoK`（現在サイトで使用中）は、LINE未インストールの方・PCブラウザの方にも配慮した公式の短縮リンクで、より汎用性が高い可能性があります。
- そのため、**今回のトップページ改修では既存の`lin.ee`リンクのまま変更していません**。実際にスマホ（LINEアプリの有無・iOS/Android両方）で動作確認したうえで、問題なければ「お急ぎの方へ」ボタンのリンク先をこちらに差し替えることをおすすめします。
- 友だち未登録の方がタップした場合の挙動（友だち追加を経てからトークが開くか等）も、実機で確認してから本採用するのが安全です。

Sources:
- [Use LINE features with the LINE URL scheme | LINE Developers](https://developers.line.biz/en/docs/messaging-api/using-line-url-scheme/)

---

## 2. リッチメニュー構成案

現状「サクッと予約」「じっくり予約」に相当する導線がWeb側に新設されたのに合わせ、リッチメニューも同じ2択を軸にした構成に整理することを提案します。

### 案A：2分割メインメニュー（シンプル・推奨）

```
┌─────────────────────┬─────────────────────┐
│                      │                      │
│   🍰 定番簡単予約     │  ✨ オーダーケーキ    │
│   最短3日後〜          │  仕様確認・予約       │
│   （タップでトーク開始） │  （仕様ページへ誘導） │
│                      │                      │
└─────────────────────┴─────────────────────┘
```

- **左（定番簡単予約）**：タップ後、上記1章の`oaMessage`URL（または通常のトーク画面）を開き、「【定番ホールケーキ予約】」の文言をベースにスタッフが即対応。
- **右（オーダーケーキ仕様確認・予約）**：タップ後、`reservation.html`（Webの仕様・ギャラリーページ）へ遷移するリンク型ボタン、または上記の「イベント・オーダーケーキ予約」プレフィル付きトークを開く。じっくり検討したい層向けなので、まずWebで情報を見てもらってからトークに誘導する設計も有効です。

### 案B：3〜4分割（情報量を少し増やす場合）

案Aの2枠に加えて、下段に以下を追加する形も検討可能です。

```
┌───────────┬───────────┐
│ 🍰 定番簡単予約│ ✨ オーダー仕様確認│
├───────────┼───────────┤
│ 📍 店舗情報   │ ❓ よくある質問  │
└───────────┴───────────┘
```

- 案Aで運用を始め、「店舗情報・アクセスを聞かれることが多い」等の実際の問い合わせ傾向を見てから、必要であれば案Bへ拡張する進め方を推奨します（最初から詰め込みすぎない）。

---

## 3. 初回あいさつメッセージ案

スマホ1画面（目安：全角100〜120字程度）に収まり、読んだ瞬間に「定番」か「オーダー」かを選んで予約チャットへ進める文面。

### 案

```
ケーキ屋さん こいまり 城東の公式アカウントです🍰
友だち追加ありがとうございます！

ご予約はこちらから選べます👇

🍰【定番ホールケーキ】
最短3日後〜、このトークでそのまま予約OK
→「定番ホールケーキ予約」と送ってください

✨【オーダーケーキ】
サイズやデコレーションにこだわりたい方は
まず下のメニュー「オーダーケーキ仕様確認・予約」をチェック！

お電話でのご相談もお気軽に
📞 070-9158-0641
```

### ポイント

- 冒頭2行で「何のアカウントか」を即座に伝える（フォロー直後に別の店だと勘違いされる事故を防ぐ）。
- 「定番」と「オーダー」を絵文字で視覚的に分け、リッチメニューの2分割構成と表現を揃える（Webのボタン文言「お急ぎの方へ」「こだわりたい方へ」とも語感を統一）。
- 直前予約（受取2日未満）は、Web側の注記と同様に電話番号を明記し、トークでの取りこぼしを防ぐ。

---

## 4. Web側との整合性まとめ

今回のトップページ改修（`index.html`の「2つの予約導線」セクション）と、本設計書のリッチメニュー・あいさつメッセージは、同じ「定番／オーダー」の2分岐を軸に統一しています。

| 接点 | 定番ホールケーキ | オーダーケーキ |
|------|----------------|----------------|
| Webのボタン文言 | 【お急ぎの方へ】定番ホールケーキの簡単ご予約 | 【こだわりたい方へ】オーダーケーキの仕様・ギャラリーを見る |
| リッチメニュー | 🍰 定番簡単予約 | ✨ オーダーケーキ仕様確認・予約 |
| あいさつメッセージ | 🍰【定番ホールケーキ】 | ✨【オーダーケーキ】 |

この一貫性により、お客様がWeb→LINEのどちらから来ても迷わない導線になります。

---

## 5. 次のアクション（オーナー側の作業）

1. LINE Official Account Manager にログイン
2. 「リッチメニュー」設定から、上記案A（または案B）のデザイン画像を作成・アップロードし、各ボタンのリンク先を設定
3. 「あいさつメッセージ」設定から、上記3章の文面を登録
4. `oaMessage`URLの動作確認（スマホ実機・LINEアプリあり/なし双方で1章のリンクをテスト）
5. 問題なければ、`index.html`の「お急ぎの方へ」ボタンのリンク先を`oaMessage`URLに差し替え（Claude側で対応可能。テスト結果を教えてください）
